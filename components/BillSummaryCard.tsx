/**
 * Back-compat export — runtime UI uses VoteCard (no AI generation).
 */
export { VoteCard as BillSummaryCardView, VoteCard as default } from "./VoteCard";
export type { VoteCardProps as BillSummaryCardProps } from "./VoteCard";
